แนวทางการจัดการระบบหลายผู้ใช้ (Multi-User)
บันทึก Refresh Token แยกตามรายบุคคล (User-specific Storage):

เมื่อผู้ใช้แต่ละคนล็อกอินและกดให้สิทธิ์แอปผ่านหน้าจอ Google OAuth เป็นครั้งแรก Backend ของคุณจะต้องรับ Refresh Token ที่ได้มา แล้วนำไปผูกบันทึกเก็บไว้ใน Database คู่กับบัญชีผู้ใช้นั้นๆ (เช่น ผูกกับ User ID หรือ Email ของเขา)

เขียนโค้ดเรียกใช้ Token ตามบัญชีของใครของมัน:

เวลาที่ผู้ใช้คนไหนก็ตามใช้งานฟังก์ชันปฏิทิน ระบบหลังบ้านจะต้องดึง Refresh Token ของ ผู้ใช้คนนั้น ออกมาจาก Database เพื่อไปขอ Access Token ชั่วคราว (อายุ 1 ชั่วโมง) มาใช้งานแบบอัตโนมัติ

วิธีนี้จะทำให้ Access Token ของใครของมันหมดอายุและต่ออายุแบบเบื้องหลัง (Background refresh) ได้เอง โดยที่ผู้ใช้ไม่ต้องกดอะไรซ้ำเลยครับ

จัดการกรณีที่ Refresh Token ใช้ไม่ได้ (Token Revocation / Expiration):

ในฐานข้อมูลควรเผื่อระบบรองรับกรณีที่ Refresh Token อาจใช้งานไม่ได้แล้ว (เช่น ผู้ใช้กด Revoke สิทธิ์, เปลี่ยนรหัสผ่าน Google, หรือไม่ได้ใช้งานนานเกิน 6 เดือน)

หากระบบหลังบ้านเรียกขอ Access Token แล้วพัง (Error) โค้ดควรจะเด้งพาผู้ใช้คนนั้นกลับไปหน้าจอขอสิทธิ์ (OAuth Consent) ใหม่เฉพาะตัวบุคคลนั้นคนเดียว ไม่กระทบผู้ใช้คนอื่นๆ ครับ