{
  "name": "times-the-calendar-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/run-tests.mjs",
    "test:browser": "node scripts/run-tests.mjs --browser",
    "test:all": "node scripts/run-tests.mjs --all",
    "verify": "npm test && npm run build",
    "verify:full": "npm run test:all && npm run build"
  },
  "dependencies": {
    "animejs": "^4.5.0",
    "firebase": "^12.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "playwright": "^1.63.0",
    "react-test-renderer": "^18.3.1",
    "vite": "^5.4.0"
  }
}
