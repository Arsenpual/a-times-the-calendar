ปรับ CSS สำหรับ Hardware Acceleration

ใส่ will-change: scroll-position และ transform: translateZ(0) ที่คอนเทนเนอร์ .tape-scroll-container เพื่อส่งภาระการประมวลผลเลื่อนจอไปให้ GPU ช่วยดึงความลื่นไหล

กำหนด scroll-behavior: auto เพื่อไม่ให้ระบบ Smooth Scroll ของ CSS ไปตีกับลูปคำนวณตำแหน่งใน JavaScript

ปรับปรุง Auto-scroll Engine ใน useEffect

ใช้ requestAnimationFrame คำนวณระยะเวลาจริงที่ผ่านไป (deltaMs) แล้วขยับ scrollTop ไปข้างหน้าตามสเกลเวลาอย่างต่อเนื่อง

แยก Logic การปรับแก้ตำแหน่งที่คลาดเคลื่อน (Drift Correction) ให้ทำงานเฉพาะเวลาคลาดเคลื่อนเกิน 5px ขึ้นไป โดยการค่อยๆ ดึงกลับแบบนุ่มนวล (drift * 0.1) ไม่ปรับพรวดพราดทุกเฟรม

ป้องกัน Event ตีกันเมื่อผู้ใช้สัมผัสจอ

ตรวจจับการ ลาก/ไถ จอผ่าน onScroll, onWheel, หรือ onTouchMove เพื่อสั่งหยุด Auto-scroll ทันที

ตั้งเวลาถอยหลัง (Idle Timeout) ไว้ 3 วินาที หลังจากหยุดลากจอ แล้วค่อยคืนการควบคุมให้ระบบ Auto-scroll ทำงานต่ออย่างราบรื่น